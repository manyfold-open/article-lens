# Copy to .env and fill in. .env is gitignored — never commit real values.

# Cloudflare API token used by `wrangler deploy` (needs Workers + KV edit).
CLOUDFLARE_API_TOKEN=

# Note: the Manyfold agent token (MF_API_TOKEN) is a *wrangler secret*, not here:
#   wrangler secret put MF_API_TOKEN
